/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['wp-tid.zillowstatic.com', 'https://photos.zillowstatic.com']
  }
}

module.exports = nextConfig
